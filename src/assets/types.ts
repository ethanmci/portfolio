export interface Experience {
    title: string
    company: string
    startEnd: string
    location: string
    tasks: Array<string>
}

export interface Education {
  education: string
  school: string
  program: string 
  startEnd: string
  location: string

}